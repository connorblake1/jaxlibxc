# Autodiff Engine

The automatic differentiation engine that computes XC derivatives (`vrho`, `vsigma`, `vtau`, `v2rho2`, ...) from energy density functions using `jax.grad` and `jax.jacfwd`.

```{eval-rst}
.. automodule:: jaxlibxc._autodiff
   :members:
   :undoc-members:
```
