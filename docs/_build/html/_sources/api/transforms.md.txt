# Variable Transforms

Converts user inputs `(rho, sigma, lapl, tau)` into internal variables `(rs, zeta, xt, xs0, xs1, u0, u1, t0, t1)` with density thresholding matching libxc.

```{eval-rst}
.. automodule:: jaxlibxc._transforms
   :members:
   :undoc-members:
```
